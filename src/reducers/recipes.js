const INITIAL_STATE = {
  categories: ['beef', 'sushi', 'pizza'],
};

const recipes = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default recipes;
