import styled from 'styled-components';

const Container = styled.div`align-items: normal;

  label {
    display: block;
    font-weight: 300;

  }

  .checked {
    text-decoration: line-through;
  }

`;

export default Container;
