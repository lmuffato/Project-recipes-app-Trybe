import styled from 'styled-components';
import SharedDropdownButton from '../../styles/shared/DropdownButton';

export const DropdownButton = styled(SharedDropdownButton)``;

export const blabla = styled.span``;
