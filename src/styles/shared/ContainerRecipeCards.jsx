import styled from 'styled-components';

const ContainerRecipeCards = styled.main`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  margin-top: 0.8rem;

  padding: 2rem 1.8rem 6rem 1.8rem;
`;

export default ContainerRecipeCards;
